# Dependencies Directory

This folder replaces the previous `requirements*` convention. All Python dependency files are now stored here, using the `dependencies*` naming scheme. This change reserves the word "requirements" for functional and non-functional requirements documentation.
